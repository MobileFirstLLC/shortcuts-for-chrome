name: Test PR

on:
  pull_request:
    branches-ignore: [ i18n ]
    paths:
      - src/*
      - test/*
      - utilities/*

jobs:
  test-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: npm ci
      - run: npm run test
